@* @model DutyFreePraxe.Models.Products *@

@* import model as an array *@
@model IEnumerable<DutyFreePraxe.Models.Products>

@{
}

    <link rel="stylesheet" href="~/css/Admin.css">

<h2>List of products</h2>

<a asp-area="" asp-controller="Admin" asp-action="Insert">Add new product</a>

@* The total amount of elements in the array *@
<p>Total amount of products: <b>@Model.Count()</b></p>

<table class="table">
    <thead>
        <tr>
            <th>
                Produkt
            </th>
            <th>
                @* obrazek *@
            </th>
            <th>
                Cena za ks
            </th>
            <th>
                Pocet na skladě
            </th>
            <th>
                @* Button1 *@
            </th>
            <th>
                @* ˇButton2 *@
            </th>

        </tr>
    </thead>
    <tbody>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    <img height="100" src=@Html.DisplayFor(modelItem => item.ImageUrl) />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Quantity)
                </td>
                <td>
                    <button class="edit">Edit</button>
                </td>
                <td>
                    <button class="delete">Delete</button>
                </td>
            </tr>
        }
    </tbody>
</table>
